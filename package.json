{
  "name": "algbnb-monorepo",
  "private": true,
  "scripts": {
    "audit:web": "npm --prefix apps/web-view run audit:web",
    "build": "npm --prefix apps/web-view run build",
    "check:mvc": "npm --prefix rout run check:mvc",
    "lint": "npm --prefix apps/web-view run lint",
    "qa:web-api": "npm --prefix rout run qa:web-api"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
